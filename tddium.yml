plan:
 - default
profiles:
  default:
    script: "apt-get install tree && cd / && tree ."
    environment:
      'FOOBARVAR': 'foobarval'
      'PATH': '/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'
    timeout: 9000
