plan:
 - default
profiles:
  default:
    script: "~/src/universal_workers_test_repo/shit.sh"
    environment:
      'FOOBARVAR': 'foobarval'
      'PATH': '/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin'
    timeout: 9000
